import type { TypertRemoteNamespaceMap } from '@deepseek-ai/dsh-typert-protocol';

/** The injected `usageAnalytics` remote namespace (undefined when mount failed). */
export type UsageRemote = TypertRemoteNamespaceMap['usageAnalytics'];
